pipeline:
  identifier: Build_praveen_host_microservice_employee_1765990920385
  name: Build microservice-employee
  orgIdentifier: default
  projectIdentifier: DemoProject002
  properties:
    ci:
      codebase:
        build: <+input>
        connectorRef: account.Github
        repoName: praveen-host/microservice-employee
  stages:
    - stage:
        identifier: build
        name: build
        spec:
          caching:
            enabled: true
          cloneCodebase: true
          execution:
            steps:
              - step:
                  identifier: echo
                  name: echo
                  spec:
                    command: echo hello world
                  timeout: ""
                  type: Run
              - step:
                  identifier: dockerbuild
                  name: docker_build
                  spec:
                    caching: true
                    connectorRef: <+input>
                    repo: hello/world
                    tags:
                      - latest
                  timeout: ""
                  type: BuildAndPushDockerRegistry
          platform:
            arch: Amd64
            os: Linux
          runtime:
            spec: {}
            type: Docker
        type: CI
